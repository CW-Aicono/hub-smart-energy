services:
  ocpp-simulator:
    build: .
    container_name: ocpp-simulator
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8090:8090"
    networks:
      - default

networks:
  default:
    name: ocpp-simulator-net
